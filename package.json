{
  "name": "login",
  "version": "0.0.3",
  "description": "A login boilerplate.",
  "author": "jasonhaxstuff <shadowmanguyyt@gmail.com>",
  "license": "Unlicense",
  "scripts": {
    "start": "yarpm run build && yarpm run start:nobuild",
    "start:nobuild": "yarpm run start:api",
    "start:api": "cd api && yarpm run start && cd ..",
    "build": "yarpm run build:database && yarpm run build:api",
    "build:database": "cd database && yarpm run build && cd ..",
    "build:api": "cd api && yarpm run build && cd .."
  },
  "devDependencies": {
    "yarpm": "^0.2.1"
  }
}
